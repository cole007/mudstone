import $ from 'jquery';
import validate from 'validate.js';

function validation(container) {
	// https://validatejs.org/
  // this needs building 
  // todo - create a utility function that wraps around validatejs
  // extract all of the heavy lifting
  // just expose a few events
}

export default validation;