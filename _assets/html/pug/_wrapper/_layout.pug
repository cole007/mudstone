include ../_mixins/mixins
block vars
doctype
<!--[if IE]>
<html lang="en" class="no-js ie">
<![endif]-->
<!--[if gt IE 9]><!-->
<html lang="en" class="no-js">
<!--<![endif]-->
    head
        meta(charset='utf-8')
        title #{pageTitle}
        meta(name='description', content='')
        meta(name='viewport', content='width=device-width, initial-scale=1')

        //- Remove script tag if modernizr is being used
        script var html=document.getElementsByTagName("html")[0];html.classList?html.classList.remove("no-js"):html.className=el.className.replace(new RegExp("(^|\\b)"+className.split(" ").join("|")+"(\\b|$)","gi")," ");
				script.
					if(!window.HTMLPictureElement || !('sizes' in document.createElement('img'))){
					!function(a){var b=function(b,c){"use strict";var d=a.document.getElementsByTagName("script")[0],e=a.document.createElement("script");return e.src=b,e.async=!0,d.parentNode.insertBefore(e,d),c&&"function"==typeof c&&(e.onload=c),e};"undefined"!=typeof module?module.exports=b:a.loadJS=b}("undefined"!=typeof global?global:this);
					document.createElement('picture');
					loadJS("/_assets/js/libs/picturefill.min.js");
					}
        // build:css
        link(rel='stylesheet', href='/_assets/css/style.css')
        // endbuild
        include ../_includes/favicons

        //- custom modernizr script, only tests for touch events
        //- script(src='/_assets/js/libs/modernizr.min.js')
    body(class=`${bodyClass !== undefined && bodyClass}`)
        .site
            include ../_includes/header
            .site-page#barba-wrapper
                .barba-container(data-namespace=`${slug}`)
                    block content
            include ../_includes/footer
            include ../_includes/source
        // build:js
        script(src='/_assets/js/dist/app.js')
        // endbuild
